export interface ICompanie {
  id: string;
  img: string;
  companie: string;
  status: string;
}

export interface INews {
  id: string;
  source: string;
  title: string;
  subtitle: string;
  createdAt: string;
  img: string;
}